node_modules/
.env

# Versehentlich eingecheckte portable Git-for-Windows-Installation.
# Enthält binäre Terminfo-Dateien, die Railway/Nixpacks beim Build crashen lassen.
Git/

# Editor / Tooling
.claude/
.vscode/
.idea/

# Termin-Daten enthalten Kunden-PII (Name, Telefon, E-Mail) und
# duerfen NIE in git landen -- weder lokale Dev-Bookings noch
# Production. data/closed-days.json bleibt absichtlich verfolgt
# (Feiertags-Kalender, kein PII).
data/appointments.json
data/appointments.json.tmp
data/backups/

# Lokale Server-Logs (start.bat redirected nach hier).
*.log
server.err.log
server.out.log
